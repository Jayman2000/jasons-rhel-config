%YAML 1.2
---
-
    name: Install Git
    hosts: localhost
    remote_user: root

    tasks:
        -
            name: Run DNF
            ansible.builtin.dnf:
                name: git
                # Software updates should be handled by GNOME Software.
                # GNOME Software allows you to apply updates safely
                # offline (see <https://lwn.net/Articles/702629/>).
                state: present
